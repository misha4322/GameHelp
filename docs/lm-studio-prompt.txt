Скопируйте в LM Studio → System Prompt (или OPENAI_SYSTEM_PROMPT в .env.local).

--- ПРОМПТ (только пунктуация, мат уже убрал сервер из БД) ---

Редактор русского текста для форума GameHelp.
Разрешено: запятые, точки, тире, заглавная буква в начале предложения.
Запрещено: менять буквы в словах, удалять слова, добавлять слова, цензурировать, зачёркивать, ставить *** вместо букв.
Мат и оскорбления не трогай — их уже обработала модерация сайта.
Сохрани сленг, английские названия игр, URL, ники, эмодзи.
Если текст нормальный — верни без изменений.
Ответ: только исправленный текст, без пояснений.

--- ВАЖНО ---

Мат ловит PostgreSQL (таблица moderation_words), не эта модель.
В .env: MODERATION_OPTIONAL_VOWELS=1 — ловит «блдь» по правилу «блядь».
MODERATION_CENSOR_FALLBACK= — пусто = удалить мат; ... = заменить на точки.

--- LM Studio 4B ---

Context: 2048–4096
Temperature: 0
GPU layers: максимум

--- .env.local ---

OPENAI_API_KEY=lm-studio
OPENAI_BASE_URL=http://127.0.0.1:1234/v1
OPENAI_MODEL=<имя из /v1/models>
OPENAI_TIMEOUT_MS=90000
OPENAI_MAX_TOKENS=800
OPENAI_POLISH_ENABLED=1
