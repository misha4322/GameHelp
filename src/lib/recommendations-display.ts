/** Сколько карточек «Мои рекомендации» на главной по умолчанию (раньше было 6). */
export const HOME_FOR_YOU_DEFAULT_LIMIT = 48;

/** Максимум за один запрос API (защита от злоупотребления). */
export const RECOMMENDATIONS_API_MAX_LIMIT = 150;

/** Визуальные «порции» на главной: столько карточек в одном блоке сетки. */
export const HOME_RECOMMENDATIONS_CHUNK_SIZE = 8;
